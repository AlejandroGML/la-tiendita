# SDD Configuration — TiendaVirtual (La Tiendita)
# Auto-generated by sdd-init on 2026-06-06

context: |
  Second-hand clothing virtual store ("La Tiendita").
  Full-stack e-commerce: Litestar (Python 3.12+, ASGI) API + Angular 18+ SPA.
  PostgreSQL 16 with SQLAlchemy async (asyncpg), Alembic migrations.
  JWT auth (access 15m + refresh 7d) + OAuth2 Google.
  i18n: Spanish, English, Swedish (ngx-translate frontend + JSON backend).
  Docker Compose for local dev.
  Project is currently in PLAN phase — no source code exists yet.

strict_tdd: false
# NOTE: No source code exists yet. strict_tdd is false because there is no
# test runner installed. Once pytest is configured for the backend and/or
# Jasmine/Karma for Angular frontend, this can be set to true.

testing:
  backend:
    runner: pytest
    runner_status: not_installed
    coverage: pytest-cov
    coverage_status: not_installed
    layers:
      unit: false
      integration: false
      e2e: false
  frontend:
    runner: Jasmine/Karma
    runner_status: not_configured
    coverage: karma-coverage
    coverage_status: not_configured
    layers:
      unit: false
      integration: false
      e2e: false

persistence:
  mode: both
  openspec_path: openspec/
  engram_project: TiendaVirtual

phases:
  propose:
    required: true
  spec:
    required: true
  design:
    required: true
  tasks:
    required: true
  apply:
    required: true
  verify:
    required: true
  archive:
    required: true

preflight:
  execution_mode: auto
  chained_pr_strategy: force-chained
  chain_strategy: stacked-to-main
  review_budget: 400
